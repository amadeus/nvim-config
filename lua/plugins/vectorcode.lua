return {
  "Davidyz/VectorCode",
  enabled = false,
  version = false, -- optional, depending on whether you're on nightly or release
  cmd = "VectorCode", -- if you're lazy-loading VectorCode
  opts = {
    async_backend = "lsp",
  },
}
